<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title>Everbetter — Quiet Ledger</title>
<style>
:root {
--bg: #0c0e10;
--bg-elev: #14181c;
--bg-panel: #181d22;
--border: #2a323a;
--border-bright: #3d4854;
--text: #e6ebe8;
--muted: #8a9690;
--dim: #5c6760;
--accent: #c4a35a;
--accent-dim: #8a7340;
--signal: #3d9b7a;
--warn: #c47a4a;
--danger: #b85c5c;
--ok: #6a9b5c;
--mono: "IBM Plex Mono", "SF Mono", "Consolas", "Liberation Mono", monospace;
--sans: "IBM Plex Sans", "Segoe UI", system-ui, sans-serif;
--radius: 4px;
}
* { box-sizing: border-box; margin: 0; padding: 0; }
html { font-size: 14px; }
body {
font-family: var(--sans);
background: var(--bg);
color: var(--text);
line-height: 1.45;
min-height: 100vh;
background-image:
linear-gradient(rgba(196, 163, 90, 0.03) 1px, transparent 1px),
linear-gradient(90deg, rgba(196, 163, 90, 0.03) 1px, transparent 1px);
background-size: 48px 48px;
}
a { color: var(--accent); text-decoration: none; }
a:hover { text-decoration: underline; }
.shell {
max-width: 960px;
margin: 0 auto;
padding: 1rem 1.25rem 3rem;
}
.topbar {
display: flex;
flex-wrap: wrap;
align-items: baseline;
justify-content: space-between;
gap: 0.75rem 1.5rem;
padding-bottom: 0.85rem;
border-bottom: 1px solid var(--border);
margin-bottom: 0.85rem;
}
.brand {
display: flex;
align-items: baseline;
gap: 0.65rem;
}
.brand h1 {
font-family: var(--mono);
font-size: 1.15rem;
font-weight: 600;
letter-spacing: 0.08em;
text-transform: uppercase;
color: var(--text);
}
.brand .tag {
font-family: var(--mono);
font-size: 0.7rem;
color: var(--accent);
letter-spacing: 0.12em;
text-transform: uppercase;
}
.nav-toggle {
display: flex;
gap: 0.35rem;
font-family: var(--mono);
font-size: 0.68rem;
letter-spacing: 0.08em;
text-transform: uppercase;
}
.nav-toggle a {
border: 1px solid var(--border);
padding: 0.25rem 0.55rem;
border-radius: var(--radius);
color: var(--muted);
text-decoration: none;
}
.nav-toggle a:hover {
border-color: var(--border-bright);
color: var(--text);
text-decoration: none;
}
.nav-toggle a.active {
border-color: var(--accent);
color: var(--accent);
background: rgba(196, 163, 90, 0.08);
}
.meta-line {
font-family: var(--mono);
font-size: 0.7rem;
color: var(--muted);
display: flex;
flex-wrap: wrap;
gap: 0.75rem 1.25rem;
align-items: center;
width: 100%;
margin-top: 0.35rem;
}
.meta-line .pill {
border: 1px solid var(--border);
padding: 0.15rem 0.45rem;
border-radius: var(--radius);
color: var(--dim);
}
.meta-line .pill.live {
border-color: var(--signal);
color: var(--signal);
}
.dna {
background: var(--bg-elev);
border: 1px solid var(--border);
border-left: 3px solid var(--accent);
padding: 0.75rem 1rem;
margin-bottom: 1.1rem;
display: grid;
grid-template-columns: auto 1fr;
gap: 0.75rem 1rem;
align-items: center;
}
.dna .label {
font-family: var(--mono);
font-size: 0.65rem;
letter-spacing: 0.14em;
text-transform: uppercase;
color: var(--accent);
white-space: nowrap;
}
.dna .motto {
font-size: 0.95rem;
font-style: italic;
color: var(--text);
opacity: 0.92;
}
.intro {
font-family: var(--mono);
font-size: 0.72rem;
color: var(--muted);
margin-bottom: 1.1rem;
letter-spacing: 0.04em;
}
.intro strong { color: var(--accent-dim); font-weight: 600; }
.ledger { display: flex; flex-direction: column; gap: 0.85rem; }
/* —— Orbit card —— */
.directive {
background: var(--bg-panel);
border: 1px solid var(--border);
border-radius: var(--radius);
overflow: hidden;
transition: border-color 0.15s;
}
.directive:hover { border-color: var(--border-bright); }
.directive.open { border-color: var(--accent-dim); }
.orbit {
padding: 0.75rem 0.9rem;
cursor: pointer;
user-select: none;
}
.orbit:focus-visible {
outline: 1px solid var(--accent);
outline-offset: -1px;
}
.orbit-top {
display: flex;
flex-wrap: wrap;
align-items: center;
gap: 0.35rem 0.55rem;
margin-bottom: 0.3rem;
}
.did {
font-family: var(--mono);
font-size: 0.68rem;
letter-spacing: 0.1em;
color: var(--accent);
font-weight: 600;
}
.dtitle {
font-size: 0.95rem;
font-weight: 600;
color: var(--text);
flex: 1;
min-width: 10rem;
}
.chips {
display: flex;
flex-wrap: wrap;
gap: 0.3rem;
margin-left: auto;
}
.chip {
font-family: var(--mono);
font-size: 0.58rem;
letter-spacing: 0.08em;
text-transform: uppercase;
padding: 0.14rem 0.4rem;
border-radius: var(--radius);
border: 1px solid var(--border);
color: var(--muted);
}
.chip.status-done { border-color: var(--ok); color: var(--ok); }
.chip.status-active