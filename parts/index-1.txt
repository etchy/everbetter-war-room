ily: var(--mono);
font-size: 0.6rem;
letter-spacing: 0.08em;
text-transform: uppercase;
padding: 0.12rem 0.4rem;
border-radius: 2px;
border: 1px solid var(--border);
color: var(--muted);
}
.badge.example {
border-color: var(--accent);
color: var(--accent);
background: rgba(196, 163, 90, 0.08);
}
.badge.status-active { border-color: var(--signal); color: var(--signal); }
.badge.status-watch { border-color: var(--warn); color: var(--warn); }
.conf-meter {
margin-left: auto;
font-family: var(--mono);
font-size: 0.65rem;
color: var(--muted);
display: flex;
align-items: center;
gap: 0.4rem;
}
.conf-bar {
width: 52px;
height: 4px;
background: var(--border);
border-radius: 2px;
overflow: hidden;
}
.conf-bar > span {
display: block;
height: 100%;
background: var(--signal);
}
.conf-bar.low > span { background: var(--danger); }
.conf-bar.mid > span { background: var(--warn); }
.story h3 {
font-size: 1rem;
font-weight: 600;
line-height: 1.35;
margin-bottom: 0.35rem;
}
.story h3 a { color: var(--text); }
.story h3 a:hover { color: var(--accent); }
.take {
color: var(--muted);
font-size: 0.88rem;
margin-bottom: 0.55rem;
border-left: 2px solid var(--border-bright);
padding-left: 0.55rem;
}
.bullets {
margin: 0 0 0.65rem 1.1rem;
color: var(--text);
font-size: 0.82rem;
}
.bullets li { margin-bottom: 0.25rem; }
.explainer {
font-size: 0.82rem;
color: var(--muted);
margin-bottom: 0.65rem;
}
.pmb {
display: grid;
grid-template-columns: repeat(3, 1fr);
gap: 0.5rem;
margin-bottom: 0.55rem;
}
@media (max-width: 700px) {
.pmb { grid-template-columns: 1fr; }
}
.pmb-cell {
background: var(--bg-elev);
border: 1px solid var(--border);
padding: 0.45rem 0.55rem;
border-radius: var(--radius);
}
.pmb-cell .k {
font-family: var(--mono);
font-size: 0.58rem;
letter-spacing: 0.1em;
text-transform: uppercase;
color: var(--accent-dim);
margin-bottom: 0.2rem;
}
.pmb-cell .v { font-size: 0.78rem; color: var(--text); }
.angles {
display: flex;
flex-wrap: wrap;
gap: 0.3rem;
}
.angle-chip {
font-family: var(--mono);
font-size: 0.58rem;
letter-spacing: 0.06em;
text-transform: uppercase;
padding: 0.1rem 0.35rem;
background: rgba(61, 155, 122, 0.12);
color: var(--signal);
border: 1px solid rgba(61, 155, 122, 0.35);
border-radius: 2px;
}
/* —— polyangle —— */
.angle-group {
margin-bottom: 0.75rem;
border: 1px solid var(--border);
border-radius: var(--radius);
overflow: hidden;
}
.angle-group:last-child { margin-bottom: 0; }
.angle-group-head {
font-family: var(--mono);
font-size: 0.65rem;
letter-spacing: 0.1em;
text-transform: uppercase;
padding: 0.4rem 0.65rem;
background: var(--bg-elev);
color: var(--signal);
border-bottom: 1px solid var(--border);
}
.angle-group ul {
list-style: none;
padding: 0.4rem 0.65rem;
}
.angle-group li {
font-size: 0.8rem;
padding: 0.3rem 0;
border-bottom: 1px solid rgba(42, 50, 58, 0.6);
color: var(--muted);
}
.angle-group li:last-child { border-bottom: none; }
.angle-group li strong { color: var(--text); font-weight: 500; }
/* —— blind spots —— */
.bs-list { list-style: none; }
.bs-list li {
display: grid;
grid-template-columns: auto 1fr auto;
gap: 0.5rem;
align-items: start;
padding: 0.45rem 0;
border-bottom: 1px solid var(--border);
font-size: 0.82rem;
}
.bs-list li:last-child { border-bottom: none; }
.sev {
font-family: var(--mono);
font-size: 0.58rem;
letter-spacing: 0.06em;
text-transform: uppercase;
padding: 0.1rem 0.3rem;
border-radius: 2px;
}
.sev.high { color: var(--danger); border: 1px solid var(--danger); }
.sev.medium { color: var(--warn); border: 1px solid var(--warn); }
.sev.low { color: var(--ok); border: 1px solid var(--ok); }
.bs-sid {
font-family: var(--mono);
font-size: 0.6rem;
color: var(--dim);
}
.avg-conf {
font-family: var(--mono);
font-size: 0.75rem;
color: var(--muted);
margin-bottom: 0.5rem;
}
/* —— forge —— */
.forge-item {
display: grid;
grid-template-columns: auto 1fr auto;
gap: 0.45rem 0.55rem;
align-items: start;
padding: 0.55rem 0;
border-bottom: 1px solid var(--border);
font-size: 0.82rem;
}
.forge-item:last-child { border-bottom: none; }
.forge-item.forge-invalid {
opacity: 0.72;
grid-template-columns: auto 1fr;
}
.forge-item .forge-body { min-width: 0; }
.forge-item .forge-title {
font-size: 0.82rem;
color: var(--text);
margin-bottom: 0.3rem;
}
.forge-fields {
display: grid;
grid-template-columns: 1fr 1fr;
gap: 0.25rem 0.55rem;
}
@media (max-width: 720px) {
.forge-fields { grid-template-columns: 1fr; }
}
.forge-field {
min-width: 0;
}
.forge-field .ff-k {
font-family: var(--mono);
font-size: 0.55re