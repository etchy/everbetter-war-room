ze: 0.58rem;
color: var(--dim);
margin-top: 0.1rem;
}
/* Ground truth — tight labeled scan blocks */
.gt-grid {
display: grid;
grid-template-columns: 1fr 1fr;
gap: 0.45rem 0.75rem;
}
@media (max-width: 640px) {
.gt-grid { grid-template-columns: 1fr; }
}
.gt-block {
min-width: 0;
padding: 0.35rem 0.45rem;
background: var(--bg-elev);
border: 1px solid var(--border);
border-radius: var(--radius);
}
.gt-block.full { grid-column: 1 / -1; }
.gt-block h5 {
font-family: var(--mono);
font-size: 0.55rem;
letter-spacing: 0.12em;
text-transform: uppercase;
color: var(--dim);
margin-bottom: 0.2rem;
display: flex;
align-items: baseline;
justify-content: space-between;
gap: 0.4rem;
}
.gt-block h5 .gt-n {
color: var(--accent-dim);
letter-spacing: 0.06em;
font-weight: 600;
}
.gt-block p, .gt-block li {
font-size: 0.76rem;
color: var(--muted);
line-height: 1.3;
}
.gt-block p {
margin: 0;
display: -webkit-box;
-webkit-line-clamp: 2;
-webkit-box-orient: vertical;
overflow: hidden;
}
.gt-block li {
display: -webkit-box;
-webkit-line-clamp: 2;
-webkit-box-orient: vertical;
overflow: hidden;
}
.gt-block.open p,
.gt-block.open li {
-webkit-line-clamp: unset;
display: block;
overflow: visible;
}
.gt-block .gt-more {
font-family: var(--mono);
font-size: 0.52rem;
letter-spacing: 0.08em;
text-transform: uppercase;
color: var(--accent-dim);
background: none;
border: none;
padding: 0.12rem 0 0;
cursor: pointer;
}
.gt-block .gt-more:hover { color: var(--accent); }
.gt-block ul { list-style: none; padding: 0; margin: 0; }
.gt-block li {
padding-left: 0.7rem;
position: relative;
margin-bottom: 0.15rem;
}
.gt-block li::before {
content: "▸";
position: absolute;
left: 0;
color: var(--accent-dim);
font-size: 0.55rem;
top: 0.1rem;
}
.gt-block .sealed {
font-size: 0.72rem;
color: var(--dim);
font-style: italic;
margin: 0;
display: block;
-webkit-line-clamp: unset;
}
.deliverable {
display: flex;
flex-wrap: wrap;
gap: 0.15rem 0.5rem;
align-items: baseline;
padding: 0.2rem 0.35rem;
background: var(--bg);
border: 1px solid var(--border);
border-radius: var(--radius);
margin-bottom: 0.22rem;
font-size: 0.74rem;
}
.deliverable:last-child { margin-bottom: 0; }
.deliverable .name { color: var(--text); font-weight: 500; }
.deliverable .path {
font-family: var(--mono);
font-size: 0.58rem;
color: var(--dim);
word-break: break-all;
display: -webkit-box;
-webkit-line-clamp: 1;
-webkit-box-orient: vertical;
overflow: hidden;
}
.gt-block.open .deliverable .path {
-webkit-line-clamp: unset;
display: block;
}
.gt-block.pressure { border-left: 2px solid var(--warn); }
.gt-block.pressure.irre { border-left-color: var(--accent); }
.gt-block.pressure.tlc { border-left-color: var(--warn); }
.empty-state {
text-align: center;
padding: 2.5rem 1rem;
color: var(--muted);
font-family: var(--mono);
font-size: 0.8rem;
}
.err {
border: 1px solid var(--danger);
color: var(--danger);
padding: 0.75rem 1rem;
border-radius: var(--radius);
font-family: var(--mono);
font-size: 0.75rem;
margin-bottom: 1rem;
}
footer.foot {
margin-top: 2rem;
padding-top: 0.85rem;
border-top: 1px solid var(--border);
font-family: var(--mono);
font-size: 0.65rem;
color: var(--dim);
letter-spacing: 0.04em;
}
</style>
</head>
<body>
<div class="shell">
<header class="topbar">
<div class="brand">
<h1>Everbetter</h1>
<span class="tag">Quiet Ledger</span>
</div>
<nav class="nav-toggle" aria-label="View toggle">
<a href="index.html">War Room</a>
<a href="ledger.html" class="active">Quiet Ledger</a>
</nav>
<div class="meta-line" id="meta-line">
<span class="pill" id="data-source">loading…</span>
<span id="updated-at"></span>
<span id="ledger-version"></span>
</div>
</header>
<section class="dna" aria-label="DNA motto">
<div class="label">DNA</div>
<div class="motto" id="dna-motto">good, better, best. Never put to rest, until good is better, and better is best.</div>
</section>
<p class="intro">
On-demand · <strong>Orbit</strong> → <strong>Flight Path</strong> → <strong>Ground Truth</strong> ·
update surface: <code>data/directives.json</code>
</p>
<div id="err" class="err" hidden></div>
<div class="ledger" id="ledger" role="list">
<p class="empty-state">Loading directives…</p>
</div>
<footer class="foot">
Quiet Ledger — not a task list. IRRE enforces never-rest. Founder has no ops role.
</footer>
</div>
<script>
(function () {
function esc(s) {
if (s == null) return "";
return String(s)
.replace(/&/g, "&amp;")
.replace(/</g, "&lt;")
.replace(/>/g, "&gt;")
.replace(/"/g, "&quot;");
}
function statusClass(st) {
return "