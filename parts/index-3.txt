-warn); font-weight: 700; }
.self-kill .sk-grid {
display: grid;
grid-template-columns: 1.15fr 1fr 0.85fr;
gap: 0.3rem 0.45rem;
}
@media (max-width: 900px) {
.self-kill .sk-grid { grid-template-columns: 1fr; }
}
.self-kill .sk-cell {
font-size: 0.68rem;
color: var(--text);
line-height: 1.3;
min-width: 0;
}
.self-kill .sk-cell strong {
font-family: var(--mono);
font-size: 0.55rem;
letter-spacing: 0.05em;
text-transform: uppercase;
color: var(--danger);
display: block;
margin-bottom: 0.1rem;
}
.self-kill .sk-cell .sk-body {
display: -webkit-box;
-webkit-line-clamp: 3;
-webkit-box-orient: vertical;
overflow: hidden;
}
.self-kill .sk-cell ul {
margin: 0.08rem 0 0 0.85rem;
padding: 0;
font-size: 0.66rem;
color: var(--muted);
}
.self-kill .sk-cell ul li {
display: -webkit-box;
-webkit-line-clamp: 2;
-webkit-box-orient: vertical;
overflow: hidden;
}
.self-kill .sk-cell .sk-note {
font-size: 0.66rem;
color: var(--muted);
margin-top: 0.15rem;
display: -webkit-box;
-webkit-line-clamp: 2;
-webkit-box-orient: vertical;
overflow: hidden;
}
.ops-age-badge {
font-family: var(--mono);
font-size: 0.58rem;
letter-spacing: 0.08em;
text-transform: uppercase;
padding: 0.1rem 0.35rem;
border-radius: var(--radius);
border: 1px solid var(--border);
color: var(--muted);
}
.ops-age-badge.fresh { border-color: var(--ok); color: var(--ok); }
.ops-age-badge.aging { border-color: var(--warn); color: var(--warn); background: rgba(196,122,74,0.1); }
.ops-age-badge.stale {
border-color: var(--danger);
color: var(--danger);
background: rgba(184,92,92,0.12);
font-weight: 700;
}
.story.ops-stale { border-color: var(--danger); box-shadow: inset 0 0 0 1px rgba(184,92,92,0.35); }
.conf-legs-glance {
margin: 0.4rem 0 0.5rem;
border: 1px dashed var(--warn);
border-radius: var(--radius);
padding: 0.4rem 0.5rem;
background: rgba(196,122,74,0.05);
}
.conf-legs-glance .clg-head {
font-family: var(--mono);
font-size: 0.58rem;
letter-spacing: 0.08em;
text-transform: uppercase;
color: var(--warn);
margin-bottom: 0.3rem;
}
.conf-legs-glance .clg-row {
display: grid;
grid-template-columns: 2.2rem 1fr;
gap: 0.35rem;
font-size: 0.7rem;
margin: 0.2rem 0;
color: var(--text);
}
.conf-legs-glance .clg-id {
font-family: var(--mono);
font-size: 0.58rem;
color: var(--warn);
letter-spacing: 0.06em;
}
.conf-legs-glance .clg-claim {
display: -webkit-box;
-webkit-line-clamp: 2;
-webkit-box-orient: vertical;
overflow: hidden;
color: var(--muted);
}
.conf-sk-glance {
font-size: 0.7rem;
color: var(--muted);
margin-bottom: 0.45rem;
line-height: 1.35;
}
.conf-sk-glance strong {
font-family: var(--mono);
font-size: 0.58rem;
letter-spacing: 0.05em;
text-transform: uppercase;
color: var(--danger);
margin-right: 0.35rem;
}
.meta-row {
display: flex; flex-wrap: wrap; gap: 0.35rem 0.75rem;
font-size: 0.7rem; color: var(--muted); margin: 0.35rem 0;
}
.meta-row strong { color: var(--text); }
.no-receipts {
font-family: var(--mono); font-size: 0.62rem; color: var(--danger);
letter-spacing: 0.06em; border: 1px solid var(--danger);
padding: 0.1rem 0.35rem; border-radius: var(--radius);
}
.cso-block {
margin-top: 0.45rem; padding-top: 0.4rem;
border-top: 1px dashed var(--border);
font-size: 0.7rem; color: var(--muted);
}
.cso-block div { margin: 0.15rem 0; }
.cso-block strong { color: var(--accent-dim); font-family: var(--mono); font-size: 0.62rem; letter-spacing: 0.04em; text-transform: uppercase; margin-right: 0.35rem; }
/* —— org pulse —— */
.roles {
display: grid;
grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
gap: 0.5rem;
}
.role-card {
border: 1px solid var(--border);
background: var(--bg);
padding: 0.55rem 0.6rem;
border-radius: var(--radius);
}
.role-card .code {
font-family: var(--mono);
font-size: 0.7rem;
letter-spacing: 0.1em;
color: var(--accent);
margin-bottom: 0.15rem;
}
.role-card .title {
font-size: 0.78rem;
font-weight: 600;
margin-bottom: 0.25rem;
}
.role-card .focus {
font-size: 0.7rem;
color: var(--muted);
}
.founder-note {
font-family: var(--mono);
font-size: 0.65rem;
color: var(--dim);
margin-top: 0.65rem;
padding-top: 0.5rem;
border-top: 1px dashed var(--border);
}
/* —— footer —— */
footer {
margin-top: 1.25rem;
padding-top: 0.75rem;
border-top: 1px solid var(--border);
font-family: var(--mono);
font-size: 0.65rem;
color: var(--dim);
display: flex;
flex-wrap: wrap;
gap: 0.5rem 1.25rem;
justify-content: space-between;
}
.empty {
color: var(--dim);
font-size: 0.85rem;
font-style: italic;
}
</style>
</head>
<body>
<div class="shell">
<header class="topbar">
<d