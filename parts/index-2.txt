m;
letter-spacing: 0.08em;
text-transform: uppercase;
color: var(--muted);
margin-bottom: 0.08rem;
}
.forge-field .ff-v {
font-size: 0.72rem;
line-height: 1.35;
color: var(--text);
display: -webkit-box;
-webkit-line-clamp: 3;
-webkit-box-orient: vertical;
overflow: hidden;
}
.forge-field.ff-kill .ff-v { color: var(--danger); }
.forge-field.ff-upgrade .ff-v { color: var(--signal); }
.forge-missing {
font-family: var(--mono);
font-size: 0.62rem;
color: var(--danger);
letter-spacing: 0.04em;
}
.forge-status {
font-family: var(--mono);
font-size: 0.58rem;
letter-spacing: 0.06em;
text-transform: uppercase;
padding: 0.12rem 0.35rem;
border: 1px solid var(--border);
color: var(--muted);
white-space: nowrap;
}
.forge-status.shipping { border-color: var(--signal); color: var(--signal); }
.forge-status.in_progress { border-color: var(--warn); color: var(--warn); }
.forge-status.proposed { border-color: var(--dim); color: var(--dim); }
.forge-status.invalid {
border-color: var(--danger);
color: var(--danger);
}
.forge-owner {
font-family: var(--mono);
font-size: 0.65rem;
color: var(--accent);
}
.story.contested {
border-color: var(--warn);
box-shadow: inset 3px 0 0 var(--warn);
}
.badge.contested {
background: rgba(196, 122, 74, 0.2);
color: var(--warn);
border: 1px solid var(--warn);
}
/* Contested + self-kill — dense at-glance strip (brass/warn + mono labels) */
.contested-glance {
margin: 0.4rem 0 0.3rem;
display: flex;
flex-direction: column;
gap: 0.35rem;
}
.contested-glance .cg-head {
font-family: var(--mono);
font-size: 0.58rem;
letter-spacing: 0.1em;
text-transform: uppercase;
color: var(--warn);
display: flex;
flex-wrap: wrap;
gap: 0.25rem 0.65rem;
align-items: baseline;
}
.contested-glance .cg-head .cg-never {
color: var(--muted);
letter-spacing: 0.06em;
}
.contested-legs {
display: grid;
grid-template-columns: 1fr 1fr;
gap: 0.35rem;
}
@media (max-width: 720px) {
.contested-legs { grid-template-columns: 1fr; }
}
.contested-leg {
border: 1px solid rgba(196, 122, 74, 0.55);
padding: 0.3rem 0.4rem;
border-radius: var(--radius);
background: rgba(196, 122, 74, 0.07);
min-width: 0;
}
.contested-leg .leg-label {
font-family: var(--mono);
font-size: 0.58rem;
letter-spacing: 0.1em;
text-transform: uppercase;
color: var(--warn);
margin-bottom: 0.12rem;
display: flex;
justify-content: space-between;
gap: 0.35rem;
align-items: baseline;
font-weight: 700;
}
.contested-leg .leg-claim {
font-size: 0.72rem;
line-height: 1.3;
color: var(--text);
margin-bottom: 0;
display: -webkit-box;
-webkit-line-clamp: 2;
-webkit-box-orient: vertical;
overflow: hidden;
}
.contested-leg .leg-meta {
font-family: var(--mono);
font-size: 0.55rem;
color: var(--muted);
letter-spacing: 0.04em;
font-weight: 500;
white-space: nowrap;
overflow: hidden;
text-overflow: ellipsis;
max-width: 55%;
}
.next-receipt {
margin-top: 0;
border: 1px solid var(--signal);
background: rgba(61, 155, 122, 0.08);
border-radius: var(--radius);
padding: 0.4rem 0.5rem;
}
.next-receipt .nr-head {
font-family: var(--mono);
font-size: 0.58rem;
letter-spacing: 0.08em;
text-transform: uppercase;
color: var(--signal);
margin-bottom: 0.22rem;
display: flex;
flex-wrap: wrap;
gap: 0.2rem 0.55rem;
align-items: baseline;
font-weight: 700;
}
.next-receipt .nr-head .nr-status-open { color: var(--warn); }
.next-receipt .nr-concrete {
font-size: 0.74rem;
line-height: 1.35;
color: var(--text);
margin-bottom: 0.2rem;
display: -webkit-box;
-webkit-line-clamp: 3;
-webkit-box-orient: vertical;
overflow: hidden;
}
.next-receipt .nr-meta {
font-size: 0.66rem;
color: var(--muted);
margin-top: 0.1rem;
display: -webkit-box;
-webkit-line-clamp: 1;
-webkit-box-orient: vertical;
overflow: hidden;
}
.next-receipt .nr-meta strong { color: var(--dim); font-weight: 600; }
.badge.forge-violation {
background: rgba(184, 92, 92, 0.32);
color: #f0a0a0;
border: 1px solid var(--danger);
font-weight: 700;
box-shadow: 0 0 0 1px rgba(184, 92, 92, 0.45);
}
.badge.live-cap {
border-color: var(--signal);
color: var(--signal);
background: rgba(61, 155, 122, 0.16);
font-weight: 700;
}
.self-kill {
border: 1px solid rgba(184, 92, 92, 0.55);
background: rgba(184, 92, 92, 0.07);
border-radius: var(--radius);
padding: 0.35rem 0.45rem;
}
.self-kill .sk-head {
font-family: var(--mono);
font-size: 0.58rem;
letter-spacing: 0.08em;
text-transform: uppercase;
color: var(--danger);
margin-bottom: 0.25rem;
display: flex;
flex-wrap: wrap;
gap: 0.2rem 0.55rem;
align-items: baseline;
font-weight: 700;
}
.self-kill .sk-outcome { color: var(-