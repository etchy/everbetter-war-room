 { border-color: var(--signal); color: var(--signal); }
.chip.status-in_progress { border-color: var(--accent); color: var(--accent); }
.chip.status-queued { border-color: var(--dim); color: var(--dim); }
.chip.status-stalled { border-color: var(--danger); color: var(--danger); }
.chip.dna-good { border-color: var(--ok); color: var(--ok); background: rgba(106,155,92,0.08); }
.chip.dna-better { border-color: var(--signal); color: var(--signal); background: rgba(61,155,122,0.08); }
.chip.dna-best { border-color: var(--accent); color: var(--accent); background: rgba(196,163,90,0.1); }
.chip.dna-stalled { border-color: var(--danger); color: var(--danger); background: rgba(184,92,92,0.1); }
.chip.dna-unset { border-color: var(--dim); color: var(--dim); }
.chip.open-n {
border-color: var(--warn);
color: var(--warn);
background: rgba(196,122,74,0.1);
font-weight: 700;
}
.chip.open-sealed {
border-color: var(--ok);
color: var(--ok);
background: rgba(106,155,92,0.06);
}
.telescope {
display: flex;
flex-wrap: wrap;
gap: 0.25rem 0.7rem;
margin: 0.2rem 0 0.35rem;
font-family: var(--mono);
font-size: 0.55rem;
letter-spacing: 0.06em;
text-transform: uppercase;
color: var(--muted);
}
.telescope span strong {
color: var(--accent-dim);
font-weight: 600;
margin-right: 0.22rem;
}
.telescope .open-n { color: var(--warn); }
.telescope .sealed-n { color: var(--ok); }
.telescope .pressure { color: var(--danger); }
.one-liner {
font-size: 0.84rem;
color: var(--text);
opacity: 0.9;
margin-bottom: 0.45rem;
line-height: 1.35;
display: -webkit-box;
-webkit-line-clamp: 2;
-webkit-box-orient: vertical;
overflow: hidden;
}
.orbit-cols {
display: grid;
grid-template-columns: 1fr 1fr;
gap: 0.55rem 1rem;
}
@media (max-width: 640px) {
.orbit-cols { grid-template-columns: 1fr; }
}
.orbit-block h4 {
font-family: var(--mono);
font-size: 0.58rem;
letter-spacing: 0.12em;
text-transform: uppercase;
color: var(--dim);
margin-bottom: 0.25rem;
}
.orbit-block ul {
list-style: none;
padding: 0;
}
.orbit-block li {
font-size: 0.78rem;
color: var(--muted);
padding-left: 0.8rem;
position: relative;
margin-bottom: 0.18rem;
line-height: 1.3;
display: -webkit-box;
-webkit-line-clamp: 2;
-webkit-box-orient: vertical;
overflow: hidden;
}
.orbit-block li::before {
content: "·";
position: absolute;
left: 0.15rem;
color: var(--accent-dim);
}
.orbit-block.open-items li::before { color: var(--warn); content: "○"; font-size: 0.55rem; top: 0.2rem; }
.orbit-block.sealed { color: var(--dim); font-size: 0.75rem; font-style: italic; }
.orbit-block .sealed {
font-size: 0.75rem;
color: var(--dim);
font-style: italic;
margin: 0;
}
.expand-hint {
font-family: var(--mono);
font-size: 0.55rem;
letter-spacing: 0.1em;
text-transform: uppercase;
color: var(--dim);
margin-top: 0.5rem;
display: flex;
align-items: center;
gap: 0.35rem;
}
.expand-hint .arrow {
display: inline-block;
transition: transform 0.15s;
color: var(--accent-dim);
}
.directive.open .expand-hint .arrow { transform: rotate(90deg); }
/* —— Depth panels —— */
.depths {
display: none;
border-top: 1px solid var(--border);
background: var(--bg);
}
.directive.open .depths { display: block; }
.depth {
padding: 0.7rem 0.9rem;
border-bottom: 1px solid var(--border);
}
.depth:last-child { border-bottom: none; }
.depth-label {
font-family: var(--mono);
font-size: 0.6rem;
letter-spacing: 0.14em;
text-transform: uppercase;
color: var(--accent);
margin-bottom: 0.5rem;
display: flex;
align-items: center;
gap: 0.45rem;
}
.depth-label .zoom {
color: var(--dim);
letter-spacing: 0.06em;
font-size: 0.55rem;
}
/* Flight path arc */
.arc {
list-style: none;
position: relative;
padding-left: 1rem;
margin: 0;
}
.arc::before {
content: "";
position: absolute;
left: 0.25rem;
top: 0.4rem;
bottom: 0.4rem;
width: 1px;
background: linear-gradient(to bottom, var(--accent), var(--border-bright));
opacity: 0.7;
}
.arc li {
position: relative;
padding: 0.22rem 0 0.4rem 0.45rem;
font-size: 0.8rem;
line-height: 1.35;
}
.arc li::before {
content: "";
position: absolute;
left: -0.88rem;
top: 0.45rem;
width: 6px;
height: 6px;
border-radius: 50%;
background: var(--bg);
border: 1.5px solid var(--accent);
}
.arc li:first-child::before {
background: var(--accent);
}
.arc li:last-child::before {
border-color: var(--signal);
}
.arc .who {
font-family: var(--mono);
font-size: 0.62rem;
color: var(--signal);
letter-spacing: 0.04em;
margin-right: 0.35rem;
font-weight: 600;
}
.arc .what { color: var(--text); }
.arc .when {
display: block;
font-family: var(--mono);
font-si